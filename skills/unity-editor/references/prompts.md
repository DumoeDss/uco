# Prompts

Use this reference to discover preset instruction prompts (slash-command style templates) the Unity plugin exposes. Resolve a prompt with `uco prompt <name>` to inject its pre-written guidance into the AI context.

Prompts do not passively consume context — they only apply when explicitly invoked.

## Live prompt groups
