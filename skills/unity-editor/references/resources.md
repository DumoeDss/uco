# Resources

Use this reference to discover read-only state resources (editor state, project info, scene hierarchy, ...) the Unity plugin exposes. Read a resource with `uco resource <uri>`.

Resources are fetched on-demand; they never modify state.

## Live resources
